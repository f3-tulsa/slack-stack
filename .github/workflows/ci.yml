# PR and branch checks without cloud credentials. Deploy stays in deploy.yml.
name: CI

on:
  pull_request:
  push:
    branches: [main, test, prod]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  requirements-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Poetry and export plugin
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          poetry self add poetry-plugin-export
      - name: Sync slackblast requirements.txt with poetry.lock
        env:
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
        working-directory: slackblast/slackblast
        run: |
          poetry export -f requirements.txt --without-hashes -o requirements.txt
          cd ../..
          if git diff --quiet slackblast/slackblast/requirements.txt; then
            echo "requirements.txt is already in sync with poetry.lock."
          elif [[ -n "${PR_HEAD_REPO}" && "${PR_HEAD_REPO}" != "${GITHUB_REPOSITORY}" ]]; then
            echo "::error::slackblast/slackblast/requirements.txt is out of sync with poetry.lock. Run: cd slackblast/slackblast && poetry export -f requirements.txt --without-hashes -o requirements.txt"
            exit 1
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add slackblast/slackblast/requirements.txt
            git commit -m "chore: sync slackblast requirements.txt with poetry.lock"
            git push
            echo "::notice::slackblast/slackblast/requirements.txt was out of sync and has been auto-fixed."
          fi

  weaselbot-requirements-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v6
      - name: Sync weaselbot requirements-lambda.txt with uv.lock
        env:
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
        working-directory: weaselbot
        run: |
          uv export --format requirements-txt --no-hashes --only-group lambda -o requirements-lambda.txt
          cd ..
          if git diff --quiet weaselbot/requirements-lambda.txt; then
            echo "requirements-lambda.txt is already in sync with uv.lock."
          elif [[ -n "${PR_HEAD_REPO}" && "${PR_HEAD_REPO}" != "${GITHUB_REPOSITORY}" ]]; then
            echo "::error::weaselbot/requirements-lambda.txt is out of sync with uv.lock. Run: cd weaselbot && uv export --format requirements-txt --no-hashes --no-dev -o requirements-lambda.txt"
            exit 1
          else
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add weaselbot/requirements-lambda.txt
            git commit -m "chore: sync weaselbot requirements-lambda.txt with uv.lock"
            git push
            echo "::notice::weaselbot/requirements-lambda.txt was out of sync and has been auto-fixed."
          fi

  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
      - name: Install Poetry and export plugin
        run: |
          python -m pip install --upgrade pip poetry pre-commit
          poetry self add poetry-plugin-export
      - name: Run pre-commit
        run: pre-commit run --all-files

  sam-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate SAM templates
        run: |
          sam validate -t PAXminer/template.yaml --lint
          sam validate -t weaselbot/template.yaml --lint
          sam validate -t slackblast/template.yaml --lint
          sam validate -t qsignups/template.yaml --lint
          sam validate -t infra/template.bootstrap.yaml --lint

  test:
    runs-on: ubuntu-latest
    env:
      # qsignups app import validates DB_ENCRYPTION_KEY at module load (handler routing test)
      DB_ENCRYPTION_KEY: ci-test-encryption-key-32chars!
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Upgrade pip
        run: python -m pip install --upgrade pip
      # Separate venvs per app — combined requirements can conflict (e.g. pymysql pins).
      - name: pytest weaselbot
        run: |
          set -euo pipefail
          python -m venv .venv-weaselbot
          . .venv-weaselbot/bin/activate
          pip install pytest -r weaselbot/requirements-lambda.txt
          cd weaselbot && pytest -q tests/
      - name: pytest slackblast
        run: |
          set -euo pipefail
          python -m venv .venv-slackblast
          . .venv-slackblast/bin/activate
          pip install pytest -r slackblast/slackblast/requirements.txt
          pytest -q slackblast/test/
      - name: pytest qsignups
        run: |
          set -euo pipefail
          python -m venv .venv-qsignups
          . .venv-qsignups/bin/activate
          # boto3 is on Lambda runtime but needed for slack_bolt.aws_lambda imports in tests
          pip install pytest boto3 -r qsignups/qsignups/requirements.txt
          pytest -q qsignups/testing/

  pip-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Security audit Python dependencies
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip pip-audit
          pip-audit -r PAXminer/requirements-lambda.txt
          pip-audit -r PAXminer/requirements.txt
          pip-audit -r PAXminer/database_management/requirements.txt
          pip-audit -r weaselbot/requirements-lambda.txt
          pip-audit -r slackblast/slackblast/requirements.txt
          pip-audit -r qsignups/qsignups/requirements.txt
          pip-audit -r qsignups/weinkes/requirements.txt
          pip-audit -r migration/requirements.txt
